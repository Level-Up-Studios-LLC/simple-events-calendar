{
  "name": "simple-events-calendar",
  "version": "3.0.0",
  "description": "A simple events calendar plugin for WordPress",
  "main": "assets/js/simple-events.js",
  "scripts": {
    "build": "echo 'Build: Copy source files to assets' && npm run copy:js && npm run copy:css",
    "copy:js": "cp src/js/*.js assets/js/",
    "copy:css": "cp src/css/*.scss assets/css/ || echo 'SCSS files copied'",
    "lint:js": "echo 'JS Linting: Run manually with a code editor'",
    "lint:css": "echo 'CSS Linting: Run manually with a code editor'",
    "lint": "npm run lint:js && npm run lint:css",
    "watch": "echo 'Watch mode: Use your IDE file watcher'",
    "dev": "npm run build",
    "dist": "rm -rf dist && mkdir dist && robocopy . dist /E /XD node_modules src .git dist /XF .gitignore package.json package-lock.json .npmrc phpcs.xml *.md /NFL /NDL /NJH /NJS || echo 'Distribution created'",
    "zip": "npm run dist && cd dist && python -c \"import zipfile, os; z = zipfile.ZipFile('../simple-events-calendar.zip', 'w'); [z.write(os.path.join(root, file), os.path.relpath(os.path.join(root, file), '.')) for root, dirs, files in os.walk('.') for file in files]; z.close()\""
  },
  "keywords": [
    "wordpress",
    "plugin",
    "events",
    "calendar",
    "acf"
  ],
  "author": "Level Up Studios, LLC",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/Level-Up-Studios-LLC/simple-events-calendar.git"
  },
  "devDependencies": {},
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead",
    "not ie 11"
  ]
}